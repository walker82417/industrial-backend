# -------------------------------
# Environment Configuration File
# -------------------------------

# MongoDB connection string (replace with your actual one in local .env)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/industrialDB?retryWrites=true&w=majority

# Port for the backend server
PORT=5000

# Optional: Secret key for JWT or encryption (future use)
JWT_SECRET=your_secret_key_here
